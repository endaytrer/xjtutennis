# Reserver plugin
#reserver-plugin:

# Challenge URL. If the reserver plugin is specified,
# challenge URL should also be specified to support
# the plugin.
#challenge-url:

# Listen Address of the web portal
listen-addr: 0.0.0.0:25571

# Database path
database-path: /var/lib/xjtutennis/xjtutennis.db